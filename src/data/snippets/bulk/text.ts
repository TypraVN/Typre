import { defineSnippets } from '../define'

/**
 * Bài bổ sung cho nhóm "text" — luyện riêng ký tự đặc biệt, không phải code chạy được.
 * Cố ý dồn dấu ngoặc, toán tử và ký hiệu shell/regex vào cùng một dòng.
 */
export const textBulk = defineSnippets('text', 'txt-x', [
  `{ } [ ] ( ) < >`,
  `! @ # $ % ^ & * ( )`,
  `- _ = + [ ] { } | \\`,
  `; : ' " , . / ? \``,
  `~ \` ! ? . , ; :`,
  `&& || ! == != === !==`,
  `< <= > >= == !=`,
  `+= -= *= /= %= **=`,
  `++ -- ** // %% ::`,
  `=> -> <- |> ::`,
  `?? ?. ?: ??=`,
  `& | ^ ~ << >>`,
  `/* block comment */`,
  `// line comment #hash --sql`,
  `<!-- html comment -->`,
  `"double" 'single' \`backtick\``,
  `\\n \\t \\r \\\\ \\" \\'`,
  `\${value} #{value} %s %d`,
  `^[a-z0-9_]{3,20}$`,
  `\\d{4}-\\d{2}-\\d{2}`,
  `(\\w+)@(\\w+)\\.(com|net)`,
  `[^\\s@]+@[^\\s@]+\\.[^\\s@]+`,
  `.*?\\s+(\\d+)\\s*$`,
  `s/old/new/g`,
  `$HOME/.config/app.json`,
  `C:\\Users\\nhat\\Documents`,
  `../../src/lib/index.ts`,
  `https://api.site.com/v1/users?id=42`,
  `key=value&flag=true#anchor`,
  `user@host:~/projects$`,
  `2 + 2 * (3 - 1) / 4`,
  `x = (a * b) % (c + d)`,
  `f(g(h(x))) + [1, 2, 3]`,
  `{ "a": [1, { "b": 2 }] }`,
  `<div id="a" class="b c">`,
  `#include <stdio.h>`,
  `array[i][j] = matrix[j][i];`,
  `map["key"] = &value;`,
  `*ptr++ = --count;`,
  `if (!ok) { return -1; }`,
  `0x1F 0b1010 0o755 1e-9`,
  `a ? b : c ?? d`,
  `... .. . , ; :`,
  `| pipe > redirect >> append 2>&1`,

  `<< >> >>> <<= >>=`,
  `|| && ?? // ** ::`,
  `!= !== <> ~= =~`,
  `#!/usr/bin/env bash`,
  `\\x41 \\u00e9 \\N{BULLET}`,
  `a[0].b["c"].d?.e`,
  `(?<name>\\w+)@(?=\\w)`,
  `--flag=value -f -- --`,
  `2>&1 1>/dev/null <<EOF`,
  `\u00ab \u00bb \u201c \u201d \u2018 \u2019 \u2014 \u2013`,
])
